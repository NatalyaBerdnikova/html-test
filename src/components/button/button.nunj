{% macro button(props) %}
  <button class="button{% if props.className %} {{ props.className }}{% endif %}" {% if props.attributes %} {{ props.attributes}} {% endif %}>
    {{ props.children }}
  </button>
{% endmacro %}
